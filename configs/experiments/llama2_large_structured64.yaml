# Paper-length diagnostic ablation: retain task structure within 64 tokens.
extends: llama2_large_multigpu.yaml
run:
  name: llama2_large_structured64
  output_dir: runs/llama2_large_structured64
  reuse_artifacts_from: runs/llama2_large_multigpu
data:
  max_input_length: 64
  prompt_truncation_strategy: structured
